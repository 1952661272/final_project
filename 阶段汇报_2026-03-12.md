# 校园二手交易平台阶段汇报（2026-03-12）

> 适用范围：本轮联调、修复与功能补充汇总  
> 汇报目标：说明本次对话期间已完成的修改、验证结果、当前系统状态，以及下一步工作重点  
> 当前项目目录：`D:\final_project\campus-trade-app`

## 一、本轮工作概述

本轮工作的重点不是新增一个孤立功能，而是围绕“系统能否真实可用”做了一次集中收口。主要完成了四类工作：

1. 对已完成功能进行补测，确认主链路是否可用。
2. 修复本地测试与构建环境，使 `npm test` 和 `npm run build` 可以直接通过。
3. 新增普通用户注册能力，并将普通用户认证接入真实 MySQL 用户表。
4. 对管理员看板、订单流转、消息中心、提醒入口等关键交互进行修复和重构。

本轮结束后，系统已经从“能演示部分流程”提升到“主要业务链路可以真实联调、真实落库、真实刷新”。

---

## 二、本轮已完成内容

### 1. 已完成功能补测与基线确认

首先对上一阶段已宣称完成的能力进行了测试，确认了以下链路是可用的：

1. 普通用户登录。
2. 商品发布后进入“待审核”。
3. 管理员审核通过后，商品在前台可见。
4. 收藏商品。
5. 下单、卖家确认、买家付款、订单状态推进。
6. 会话创建、消息发送、消息读取。
7. 管理员登录、用户实名认证、管理员禁用用户。

这一步的意义在于，先保证已有内容没有明显问题，再继续扩展功能，避免在不稳定基线上继续堆叠需求。

### 2. 修复测试与构建环境

对本地运行环境进行了整理，解决了以下问题：

1. `npm test` 之前因为依赖缺失无法执行，现已恢复为可直接运行。
2. `npm run build` 之前受系统默认 Node 版本影响失败，现已改为显式使用项目内置 Node 22，可直接通过。
3. 补齐了 `vitest`、`supertest` 等测试依赖，确保自动化测试环境完整。

当前标准命令状态：

1. `npm test` 可直接执行通过。
2. `npm run build` 可直接执行通过。

### 3. 新增普通用户注册，并接入真实数据库

本轮新增了完整的普通用户注册能力，普通用户认证已不再只依赖临时 JSON 状态。

已完成内容：

1. 新增 `POST /api/v1/auth/register` 接口。
2. 普通用户登录由“按名字登录”改为“按学号 + 密码登录”。
3. 注册字段为：姓名、学号、密码。
4. 注册成功后写入真实 MySQL 用户表 `ct_user`。
5. 登录校验也基于 `ct_user.password_hash` 完成，不再使用明文密码。
6. 启动 MySQL 模式时，会初始化认证相关表，并保留演示账号可用。
7. 新注册或登录的用户，会同步进入 `app_state.users`，以兼容当前商品、订单、消息等业务流。

需要特别说明的是：

1. 普通用户认证已经接入真实 MySQL 用户表。
2. 但商品、订单、消息、系统通知等核心业务数据，目前仍主要通过 `app_state` 保存。
3. 当前后端运行在 MySQL 模式下时，`app_state` 也是持久化到 MySQL 的，而不是落在本地文件里。

也就是说，目前系统处于“用户认证真实落表，业务状态先通过 app_state 持久化到 MySQL”的过渡阶段。

### 4. 补全前端注册与登录体验

围绕新注册能力，前端同步完成了配套改造：

1. 新增注册页 `/#/register`。
2. 登录页改为“学号 + 密码”登录。
3. 注册页支持姓名、学号、密码、确认密码输入。
4. 注册成功后自动进入登录态。
5. 登录页增加“去注册”入口，注册页增加“返回登录”入口。
6. 前端状态管理增加 `register` 方法，并同步兼容已有用户态逻辑。

### 5. 修复管理员用户管理与数据看板

本轮针对管理员页面做了多次修复，核心问题已经收口。

已修复内容：

1. 用户管理页之前被默认分页限制，每页只显示 5 条，导致新注册用户看不到。
2. 现已改为完整显示，并按注册时间倒序展示。
3. 用户管理页补充了“学号”展示，便于管理员识别新注册用户。
4. 数据看板中的“活跃用户”统计口径不合理，现已改为更符合当前需求的“用户总数”。
5. 数据看板原先存在取数来源不一致的问题，现已改为后台真实统计接口驱动。
6. 发布商品、审核通过、创建订单、付款等行为，已经可以真实影响管理员看板的统计。

### 6. 修复商品下单与付款链路

本轮对订单链路进行了重要修复：

1. 当商品存在进行中订单时，前台会显示为“交易中”，避免仍误导用户点击下单。
2. 卖家确认订单后，买家可以选择付款方式进行支付。
3. 已补充付款接口与前端付款入口。
4. 订单状态会在“待确认 -> 已确认 -> 进行中 -> 已完成/已取消”等状态间流转。
5. 管理员看板中的成交订单与成交额，会随真实订单变化而更新。

这部分修复后，商品详情、订单列表、管理员统计三者之间的数据联动已经更一致。

### 7. 重构消息中心与提醒入口

本轮对消息模块做了比较大的结构调整，已经改成“先聊天，再购买”的业务流程。

已完成内容：

1. 商品详情页的“我想要”不再直接创建订单，而是先创建或打开会话。
2. 点击“我想要”后会跳转到消息中心，并自动定位到对应商品会话。
3. 真正的“购买”按钮移动到了聊天详情区域。
4. 聊天会话与系统消息在同一消息列表中混排展示。
5. 消息页支持显示商品卡片、聊天记录、会话未读数、系统通知内容。
6. 系统通知已覆盖以下事件：
   - 商品提交审核
   - 商品审核通过
   - 商品审核驳回
   - 买家创建订单
   - 卖家确认订单
   - 卖家拒绝订单
   - 买家付款
   - 订单完成
   - 订单取消
7. 新增通知接口与已读接口：
   - `GET /api/v1/notifications`
   - `PATCH /api/v1/notifications/:id/read`
8. 新增会话级已读接口：
   - `PATCH /api/v1/conversations/:conversationId/read`
9. 顶部“提醒”改为统一消息入口：
   - 桌面端鼠标悬停可显示小弹窗预览
   - 点击后进入消息页
   - 未读聊天、待处理订单、未读系统通知统一进入这里

至此，消息模块不再只是一个空白输入框页面，而是已经成为与商品、订单、提醒联动的真实业务中心。

---

## 三、本轮重点问题与修复结论

### 1. 注册功能缺失

问题：

系统原先没有新用户注册能力，无法支撑真实用户增长。

修复结论：

已补上注册页、注册接口、真实数据库用户写入、学号登录、密码校验等完整能力。

### 2. 注册后管理员页显示异常

问题：

管理员页曾出现测试脏数据、分页隐藏但实际仍限制条数、新注册用户看不到等问题。

修复结论：

已清理调试残留数据，修复用户表显示逻辑，并补充学号字段展示。

### 3. 数据看板与真实业务行为脱节

问题：

管理员数据看板部分数字没有根据真实商品和订单变化而更新。

修复结论：

现已改为读取后台真实统计数据，商品发布、审核、下单、付款等行为会联动更新统计结果。

### 4. “我想要”与购买流程不合理

问题：

旧流程是点击商品“我想要”直接下单，不符合先沟通后购买的校园二手场景。

修复结论：

已调整为“我想要 -> 进入聊天 -> 沟通确认 -> 在聊天页购买”的流程。

### 5. 消息页不可用、提醒入口不可用

问题：

消息页原先几乎不可实际使用，提醒入口也缺乏与消息中心的有效绑定。

修复结论：

现已重构为统一收件箱视图，并用真实的未读消息、系统通知和订单提醒驱动提醒入口。

---

## 四、测试与验证结果

本轮已经完成的验证包括：

### 1. 自动化测试

当前结果：

1. `npm test` 通过。
2. 后端测试当前共通过 `12` 项。
3. MySQL 相关的可选测试在缺少专用测试连接串时会跳过，不影响主流程。

本轮新增或加强覆盖的测试点包括：

1. 新用户注册与读取当前用户信息。
2. 重复学号注册拦截。
3. 学号登录与禁用用户登录拦截。
4. 商品发布与审核上架。
5. 重复活跃订单拦截。
6. 卖家确认、买家付款。
7. 管理员看板统计联动。
8. 会话复用、消息发送、消息已读。
9. 系统通知生成与通知已读。

### 2. 构建验证

当前结果：

1. `npm run build` 通过。
2. 前端 SPA 可成功构建输出到 `dist/spa`。

### 3. 运行验证

当前本地运行状态：

1. 前端开发服务运行在 `http://127.0.0.1:9000/`
2. 后端服务运行在 `http://127.0.0.1:3001/`
3. 后端健康检查可正常返回。
4. 当前后端以 MySQL 模式运行。

---

## 五、当前系统状态说明

为了避免汇报口径不清，需要明确区分“已完成”与“尚在过渡”。

### 1. 当前已经可以明确说已完成的内容

1. 普通用户注册与登录。
2. 普通用户认证真实接入 MySQL 用户表。
3. 商品发布、审核、展示、收藏、下单、付款、订单流转。
4. 管理员商品审核、用户管理、数据看板。
5. 消息会话、系统消息、提醒预览。
6. 标准测试与构建命令恢复可用。

### 2. 当前仍属于过渡实现的部分

1. 用户认证已经是真实数据库表。
2. 但商品、订单、消息、通知目前还没有完全拆分为关系型业务表。
3. 这些业务数据当前仍由 `app_state` 统一维护，再持久化到 MySQL。

因此，当前更准确的表述应该是：

**系统已实现“真实注册登录 + 真实后端联调 + MySQL 模式持久化运行”，但商品、订单、消息等业务表的彻底关系化改造仍是下一阶段工作。**

---

## 六、下一步需要做什么

接下来建议按“先补底层真实性，再做体验优化”的顺序推进。

### 1. 完成业务数据从 app_state 到真实业务表的迁移

优先级最高。

建议下一阶段重点完成：

1. 商品写入真实商品表。
2. 商品图片、标签写入关联表。
3. 订单写入真实订单表与订单状态日志表。
4. 会话与消息写入真实消息相关表。
5. 系统通知写入独立通知表。

完成后，系统才能从“半关系化过渡态”进入“完整关系数据库实现”。

### 2. 完善消息系统实时性

当前提醒和消息已可用，但仍然是轮询刷新机制。

建议后续优化：

1. 引入 WebSocket 或 SSE，实现消息实时推送。
2. 实现更细致的未读数同步。
3. 增加会话搜索、会话置顶、消息时间分组。

### 3. 完善订单与支付体验

当前支付属于站内状态流转模拟，适合毕业设计演示，但仍有优化空间。

建议后续补充：

1. 更明确的支付页与支付状态回显。
2. 订单详情页。
3. 买卖双方操作日志展示。
4. 拒绝订单原因、取消订单原因展示。

### 4. 完善管理员治理能力

目前管理员链路已经可用，但可以继续增强：

1. 用户管理增加按学号、状态、认证情况筛选。
2. 商品审核增加驳回原因输入和查看。
3. 看板增加按日期筛选与更细的订单分组统计。
4. 增加系统通知管理与违规内容处理能力。

### 5. 提升整体可靠性与可维护性

建议继续做以下工程化优化：

1. 增加前端关键页面的交互测试。
2. 补充更多 MySQL 集成测试。
3. 对核心流程补充错误码与异常场景测试。
4. 清理调试遗留数据与临时日志文件。
5. 为接口、状态流转和数据库映射补充正式文档。

---

## 七、建议后续优化重点

如果需要给导师或答辩汇报一个更聚焦的“优化方向”，建议概括为以下三点：

1. **数据层优化**：把商品、订单、消息、通知从 `app_state` 完整迁移到真实 MySQL 业务表。
2. **交互层优化**：提升消息中心、订单页、管理员看板的实时性与可操作性。
3. **工程层优化**：继续补自动化测试、补接口文档、整理部署与运行文档。

---

## 八、本轮结论

本轮工作不是单点修补，而是完成了一次较完整的可用性收口：

1. 把注册登录真正接入了真实数据库。
2. 把管理员看板、订单统计和业务行为重新对齐。
3. 把消息中心从不可用页面改造成真实业务入口。
4. 把“提醒”改造成与消息和通知联动的统一入口。
5. 把测试与构建环境恢复到可直接执行通过。

因此，本轮可以认为已经完成了：

**“认证真实化、交易链路补全、消息提醒重构、管理端联动修复、测试构建恢复”这一组核心收口任务。**
