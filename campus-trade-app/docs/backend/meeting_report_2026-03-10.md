# 校园二手交易平台阶段汇报（2026-03-10）

> 适用场景：周四阶段会议汇报  
> 当前版本口径：按项目真实代码与联调结果整理  
> 汇报重点：当前已完成内容、当前系统状态、下个环节计划

## 一、当前阶段目标

本阶段的核心目标有两个：

1. 优先完成数据库表设计收口，确保数据库结构与前台已实现功能匹配。
2. 在数据库设计稳定的前提下，开始搭建后端服务，并把前台与后台管理页面接入真实后端接口。

---

## 二、当前已完成内容

### 1. 数据库设计已完成本阶段收口

已完成内容：

1. 完成数据库设计文档编写与建表脚本整理。
2. 已形成正式建表 SQL 文件：
   [schema_mysql.sql](/Users/mac/Downloads/毕设产品说明书/campus-trade-app/docs/database/schema_mysql.sql)
3. 已形成数据库设计说明文档：
   [database_design.md](/Users/mac/Downloads/毕设产品说明书/campus-trade-app/docs/database/database_design.md)
4. 已根据前台真实字段补齐商品标签能力，新增 `ct_listing_tag`，解决了前台商品 `tags` 无法落库的问题。

当前结论：

数据库设计已经完成本阶段要求的“前台功能对表”收口，核心业务实体已覆盖用户、商品、商品图片、商品标签、审核、收藏、订单、订单状态日志、会话、消息等模块。

### 2. 后端服务已完成第一阶段搭建

已完成内容：

1. 后端从原来的单文件原型，重构为 `app + repository + service` 的领域结构。
2. 已完成认证、用户、商品、收藏、审核、订单、消息等核心 API。
3. 后端已支持 `json` 模式运行，当前通过本地持久化文件保存业务数据。
4. 后端已预留 `MYSQL_URL` 模式，后续可以切换到 MySQL 数据库。

相关代码文件：

1. [app.js](/Users/mac/Downloads/毕设产品说明书/campus-trade-app/backend/app.js)
2. [service.js](/Users/mac/Downloads/毕设产品说明书/campus-trade-app/backend/service.js)
3. [repository.js](/Users/mac/Downloads/毕设产品说明书/campus-trade-app/backend/repository.js)
4. [server.js](/Users/mac/Downloads/毕设产品说明书/campus-trade-app/backend/server.js)

### 3. 前后端已经完成真实联调

已完成内容：

1. 前台页面不再单纯依赖本地 mock 状态，而是通过真实后端 API 获取和提交数据。
2. 管理员页面已经具备真实审核能力。
3. 已验证“用户发布商品 -> 管理员审核 -> 前台显示商品”的联动逻辑。
4. 已修复联调过程中的关键问题：
   - 用户发布商品时请求体丢失，导致标题为空、价格为 0。
   - 管理员端与前台端刷新不及时，导致审核结果无法及时反馈。

### 4. 管理员页面已具备真实反馈能力

当前管理员页面已支持：

1. 查看待审核商品。
2. 对新发布商品执行通过或驳回。
3. 审核结果同步影响前台商品是否可见。
4. 用户端和管理员端通过自动刷新机制保持状态同步。

管理员入口：

1. 前端后台登录页：`/#/admin/login`
2. 后台看板页：`/#/admin/dashboard`
3. 商品审核页：`/#/admin/listings`
4. 用户管理页：`/#/admin/users`

### 5. 测试基线已建立

已完成内容：

1. 已引入 `Vitest + Supertest`。
2. 已完成后端核心 API 测试。
3. 已补数据库 schema 合约测试。
4. 已补可选的 MySQL schema 冒烟测试。

当前验证结果：

1. `npm test` 通过。
2. `npm run build` 通过。
3. 前后端开发服务可正常启动。

---

## 三、当前系统实际状态

这一部分建议会议中重点说明，避免口径不清。

### 1. 当前已经完成的真实链路

当前系统已经实现并验证的链路为：

1. 用户登录
2. 商品发布
3. 管理员审核
4. 商品在前台显示/隐藏
5. 收藏
6. 下单
7. 订单状态流转
8. 私聊消息

### 2. 当前存储模式

当前系统不是直接写入 MySQL，而是：

```text
前端页面 -> 后端 API -> backend/data/state.json
```

也就是说：

1. 当前已经完成“前后端真实打通”。
2. 当前尚未完成“真实 MySQL 落库”。
3. 当前数据持久化介质是 [state.json](/Users/mac/Downloads/毕设产品说明书/campus-trade-app/backend/data/state.json)。

### 3. 当前不能夸大表述的部分

会议汇报时不应表述为：

1. “数据库已经正式接入运行”
2. “所有业务数据已经写入 MySQL”
3. “支付、评价、通知中心已经完成”

因为这些内容在当前阶段并未完成。

---

## 四、本阶段产出总结

本阶段的实际产出可以概括为：

1. 数据库表设计已完成收口，并补齐前台字段缺口。
2. 后端服务已经从原型升级为可联调、可测试的业务服务。
3. 用户前台与管理员后台已经通过后端打通。
4. 管理员审核已经能够真实影响前台商品展示。
5. 已完成基础自动化测试和构建验证。

因此，本阶段可以认定为：

**已完成“数据库设计优先收口 + 后端第一阶段搭建 + 前后端主流程联调”三项核心任务。**

---

## 五、下个环节计划

下个环节的重点，不再是前后端是否能联动，而是把当前联动结果正式落到 MySQL。

### 1. 切换到 MySQL 模式

目标：

1. 配置 `MYSQL_URL`
2. 在真实 MySQL 实例上执行建库建表
3. 后端切换为 MySQL 模式运行

预期结果：

1. 后端不再主要依赖 `state.json`
2. 核心业务数据能够真实进入数据库表

### 2. 完成业务数据与数据库表的真实映射

重点完成：

1. 商品、图片、标签写入商品相关表
2. 审核记录写入审核表
3. 订单写入订单表和订单状态日志表
4. 会话和消息写入消息相关表

### 3. 做 MySQL 模式下的全链路回归

验证链路：

1. 发布 -> 审核 -> 前台显示
2. 详情 -> 下单 -> 订单状态流转
3. 私聊 -> 消息发送 -> 已读更新

### 4. 输出数据库接入证据

下一阶段需要形成可汇报材料，包括：

1. 数据库表结构截图
2. 关键表样例数据
3. MySQL 模式运行截图
4. 前后台联动截图

### 5. 更新论文与汇报文档口径

在 MySQL 真正接入后，再把文档口径更新为：

1. 数据已落入真实数据库
2. 核心业务链路已完成持久化
3. 可展示数据库层面的系统实现成果

---

## 六、当前阶段未覆盖内容

这部分建议汇报时主动说明，避免老师追问时被动。

当前阶段未覆盖：

1. MySQL 正式接入运行
2. 保存草稿持久化
3. 评价系统
4. 通知中心
5. 支付功能
6. 注册/找回密码

这些内容属于后续阶段任务，不属于当前阶段验收重点。

---

## 七、会议可直接口头汇报版本

本阶段我主要完成了三项工作。第一项是数据库设计收口，我已经根据前台真实功能把数据库表结构补齐，尤其补上了商品标签表，确保数据库设计和前台功能是对齐的。第二项是后端服务搭建，我把后端从原来的原型代码重构成了可联调、可测试的服务结构，并完成了认证、商品、审核、订单、消息等核心接口。第三项是前后端联调，我已经实现了用户前台和管理员后台通过真实后端联动，当前可以完成“用户发布商品，管理员审核，前台显示商品”的完整闭环。

需要特别说明的是，当前阶段虽然已经完成前后端真实打通，但数据暂时还是先写入本地的 JSON 持久化文件，还没有正式切换到 MySQL。这是下一阶段的重点工作。下一阶段我会把后端存储切换到 MySQL 模式，验证商品、审核、订单、消息等数据真正写入数据库表，并完成数据库模式下的全链路回归测试。

---

## 八、建议的 PPT 页面结构

如果周四需要做 PPT，可按下面 5 页组织：

1. 本阶段目标
   - 数据库设计收口
   - 后端第一阶段搭建
   - 前后端主流程联调

2. 已完成内容
   - 数据库表设计补齐
   - 后端接口完成
   - 管理员审核联动完成

3. 当前系统状态
   - 前端 -> 后端 -> JSON 持久化
   - 尚未切到 MySQL

4. 当前成果演示
   - 用户发布
   - 管理员审核
   - 前台显示

5. 下阶段计划
   - 切换 MySQL
   - 业务表真实落库
   - 数据库模式全链路回归
