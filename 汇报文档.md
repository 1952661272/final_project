# 校园二手交易平台阶段汇报（终稿）

> 汇报时长：约 5 分钟  
> 汇报定位：全链路落地成果汇报  
> 汇报版本：V2.0

## 一、本期核心完成内容（约60秒）
1. 完成前端、后端、数据库全链路贯通，系统进入可稳定演示与可持续迭代阶段。
2. 完成分类导航功能化，分类入口支持路由跳转：`/#/category/{分类名}`。
3. 完成快速筛选参数化跳转：`/#/search?price=0-100&sort=价格升序`。
4. 完成订单支付、履约、售后流程，支持交易全周期追踪。
5. 完成后台审核、订单治理、用户治理与风控预警闭环。

## 二、系统闭环演示路径（约2分30秒）
1. 学生端登录并完成校园认证。
2. 从首页进入分类导航，点击“数码/教材”等分类，跳转 `/#/category/数码`。
3. 点击“低价捡漏”等快捷入口，跳转 `/#/search?price=0-100&sort=价格升序`。
4. 在搜索页组合筛选并进入商品详情，发起咨询与收藏。
5. 提交订单并完成支付，查看订单状态从“待支付”流转到“待发货/待收货/已完成”。
6. 进入消息中心查看买卖双方会话与通知。
7. 管理员登录后台，执行商品审核、风险订单处理与用户治理。

## 三、关键问题与修复结论（约50秒）
1. 问题一：早期版本存在页面层级冲突，出现大面积空白下沉。
2. 修复一：统一布局容器语义，重置全局 z-index 策略，页面恢复稳定。
3. 问题二：筛选状态在跳页后丢失，导致结果不可复现。
4. 修复二：采用 `params + query` 双向联动并回写 URL，筛选结果可复现可分享。
5. 问题三：订单支付回调存在重复处理风险。
6. 修复三：支付回调引入幂等控制与流水校验，消除重复记账问题。

## 四、当前能力与数据支撑（约50秒）
1. 技术栈：Quasar + Vue3、Spring Boot + MyBatis、MySQL + Redis。
2. 业务能力：认证、发布、搜索、下单、支付、履约、消息、审核、风控。
3. 数据能力：核心业务表已完成持久化，支持订单日志、审核日志、支付流水追踪。
4. 性能情况：核心接口平均响应在目标阈值内，主流程稳定。

## 五、下一阶段工作（约40秒）
1. 完成推荐策略升级，引入基于行为特征的排序优化。
2. 完成风控模型迭代，提升异常行为识别精度。
3. 增强自动化测试覆盖率，建立持续集成回归机制。
4. 输出答辩版本文档与演示脚本，保证论文、说明书、汇报口径一致。

---

## 5 分钟口播稿（可直接照读）
各位老师好，我汇报的项目是校园二手交易平台。当前版本已经完成前端、后端和数据库的全链路打通，系统具备从用户登录到交易完成再到后台治理的完整闭环能力。

本期最重要的升级是把分类导航做成真正可用的功能入口。现在点击分类卡片会直接跳到对应路径，例如 `/#/category/数码`。同时，快速筛选入口支持参数化跳转，例如 `/#/search?price=0-100&sort=价格升序`。搜索页会根据 URL 自动回显筛选条件，这样筛选结果可以复现和分享。

交易流程方面，系统已经具备下单、支付、履约和售后处理能力。用户在详情页可以发起咨询、收藏和下单，支付成功后订单状态会按状态机流转。卖家和买家可以在消息中心保持沟通，管理员可以在后台完成商品审核、风险订单处理和用户治理。

在问题修复上，我们解决了三个关键问题：第一，页面层级冲突导致的空白下沉，通过统一布局语义和 z-index 策略已修复；第二，筛选状态丢失，通过 params 与 query 联动并回写 URL 已解决；第三，支付回调重复处理风险，通过幂等控制和流水校验已消除。

当前系统技术栈为 Quasar 与 Vue3 前端、Spring Boot 与 MyBatis 后端、MySQL 与 Redis 数据层。核心业务链路稳定，数据可追溯，支持答辩演示和后续迭代。下一阶段我们将重点做推荐优化、风控模型增强和自动化测试覆盖。我的汇报结束，谢谢各位老师。

